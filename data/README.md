`example.json` is the chart the app loads on first run (`js/file-manager.js` `loadInitialData()`) — it's also the reference example for [`docs/dsl.md`](../docs/dsl.md), exercising every DSL feature at once (a strict hierarchy, one co-led position, one staff position, one collapsed node).
